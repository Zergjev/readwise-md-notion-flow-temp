# Google Drive credentials
GOOGLE_SERVICE_JSON=  # Either JSON or base64-encoded service account credentials
DRIVE_FOLDER_ID=      # Your Google Drive folder ID

# Optional output directory (local path for downloads)
AUDIO_OUTPUT_DIR=downloads/audio

